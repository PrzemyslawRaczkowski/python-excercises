#!/usr/bin/env python3.7

import random

def generateList():
    size = random.randint(5, 10)
    generatedNumbers = []
    while size >= 0
        generatedNumbers.append(random.randint(5, 10))
    return 

